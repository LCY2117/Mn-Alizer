你是顶级产品设计师和体验设计师。请基于我提供的 JSON 数据，生成一套高保真、适合答辩展示、同时具备后续前端转码潜力的桌面 Web Demo 设计稿。

重要前提：
1. 我会提供一个 JSON 文件，JSON 是本次设计的唯一数据源。
2. 所有页面内容、图表数值、标签、推荐结果、ROI、买家列表、碳收益，都必须优先来自 JSON。
3. 允许你根据 JSON 做合理的视觉表达、信息分组、图表形式选择、叙事增强和美学发挥。
4. 不要机械照搬表格；请把数据转化成一个有说服力、适合路演和演示的“工业智能 + AI 决策 + 商业闭环”系统体验。
5. 不要随意编造关键业务数字。如果 JSON 某项为 null 或缺失，请弱化展示、留空、或用“待补充数据 / Demo 模拟”表达，不要伪造精确值。
6. 可以基于 JSON 中已有数值进行数学推导、汇总、排序、匹配展示，但不能改变原始事实含义。
7. 请默认中文界面，局部可以使用少量英文专业词汇，例如 Telemetry、AI Engine、ROI、Digital Twin、CCER。

设计目标：
我要的是一个“电解锰渣智能分质利用系统”的桌面大屏 Demo。这个系统面向政府、产业方、投资人、评委，核心要表达：
- 它能替代高危人工勘探
- 它能做数据诊断和 AI 决策
- 它能自动推荐锰渣资源化利用方向
- 它能证明方案合规、安全、有效
- 它能算清楚经济账
- 它能形成供需撮合和碳资产闭环

整体风格要求：
- 工业科技感、可信赖、适合比赛答辩和项目路演
- 不是传统政务页面，也不是消费级花哨 App
- 更偏“智慧工业中台 / AI 决策驾驶舱 / 新质生产力平台”
- 视觉上允许你自由发挥，不必拘泥于死板布局
- 但要保证信息清晰、层级明确、重点突出、适合演示讲故事
- 适合 1440px 左右桌面端展示
- 深色或浅色均可，由你决定，但必须高级、专业、有未来感
- 图表、卡片、地图、状态流、徽章、进度条等可以自由组合
- 页面要具备明显的“从现场到价值闭环”的递进关系

请设计 5 个核心模块 / 页面（可以是 5 个主页面，也可以是 1 套主框架下的 5 个重点视图）：

模块一：边缘感知 —— 全地形小车勘探与数据采集端
目标：
- 展示无人小车在锰渣堆场原位采样、替代人工高危作业
- 强调实时性、现场感、感知能力、边缘智能
建议包含但不限于：
- 小车状态 / 遥测面板
- 第一视角画面区域
- 堆场扫描 / 轨迹 / 孪生可视化
- 实时采集参数流
- 采样点位、坐标、电量、坡度、湿度、pH、锰、氨氮、粒径等
要求：
- 数据必须引用 JSON 中 batch、sampling 等字段
- 允许你用“勘探中”“已采样”“上传中”等状态增强演示感
- 如果 JSON 中没有某些视觉素材，请用高质量占位设计表达，不要破坏整体高级感

模块二：云端中枢 —— 数据同步与 AI 初始诊断
目标：
- 把现场采样数据转化为可理解的风险诊断
- 让用户快速明白这批锰渣的问题在哪里
建议包含但不限于：
- 数据同步 / 上传 / 分析进度表现
- AI 初诊结果
- 风险等级
- 风险雷达图或多维分析图
- 预处理建议
要求：
- 诊断逻辑和风险维度来自 JSON 中 diagnosis
- 要体现“从原始参数到可执行判断”的转换
- 页面要有明显的 AI 感，但不能做得像纯概念图

模块三：AI 大脑 —— 智能分质调度与配方寻优
目标：
- 这是系统的核心壁垒
- 展示系统如何从多种去化方向里挑出最优方案，并输出具体配方
建议包含但不限于：
- 四类材料方向的匹配度对比
- 当前推荐路线高亮
- 推荐理由
- BOM / 配方卡片 / 工艺建议
- 可切换查看不同材料方向
要求：
- 四类材料必须全部展示，数据来自 JSON 的 materials
- 推荐项必须依据 JSON 的 is_recommended / match_score
- 要让用户一眼看出“为什么选这个，不选另外几个”
- 不需要过度工程化解释算法，但要有“AI 决策可信”的感觉

模块四：数字孪生 —— 性能预测与经济价值核算
目标：
- 证明方案不是纸上谈兵
- 同时证明它合规、有效、并且划算
建议包含但不限于：
- 强度发展曲线
- 合规认证 / 标准徽章
- 浸出达标、路基适配或胶凝性能表现
- 传统处理成本 vs 本方案成本
- 单吨成本、总成本、节省金额、毛利/收益空间
要求：
- 性能数据来自 JSON 的 performance
- ROI 数据来自 JSON 的 roi
- 请把“合规可信”和“经济更优”做成这一页最强的信息锚点
- 可以适度做出“财务仪表盘 + 技术验证”的复合体验

模块五：商业闭环 —— 供需撮合与碳资产管理
目标：
- 展示这不是实验室技术，而是可进入市场的产业闭环方案
建议包含但不限于：
- 周边需求方 / 潜在客户 / 买家列表
- 地图、雷达、匹配度、运输距离、运输成本
- 一键对接 / 派单 / 合同生成感
- 碳减排、碳收益、CCER/碳资产相关表达
要求：
- 买家和距离等来自 JSON 的 buyers
- 碳减排和收益来自 JSON 的 carbon
- 要把“处理掉锰渣”升级为“卖得出去、还能形成低碳价值”的商业闭环
- 这是答辩中非常关键的一页，请设计得更有商业说服力

全局体验要求：
- 整套设计要形成一条清晰的用户旅程：
  现场勘探 -> 数据上传 -> AI 诊断 -> 配方推荐 -> 性能/ROI 验证 -> 买家撮合/碳资产
- 允许你设计统一侧边导航、顶部批次栏、步骤导航、状态标签等系统化元素
- 页面间要有明显一致性，但每一页都应有自己的视觉重点
- 不要做得像 5 张无关海报，要像一个完整产品
- 可以适度加入微交互暗示，例如加载、匹配计算、推荐高亮、合同生成、地图脉冲点等
- 重点不是静态海报，而是“可运行 demo 的高保真界面感”

数据与文案规则：
- JSON 中的真实调研数据、文献数据、市场数据、demo mode，请通过视觉方式适度区分
- 对 source_type 可用轻量标记或脚注样式表达，例如：
  - 官方/文献/市场调研/demo mode
- 但不要让页面变成学术引用堆砌
- 必须保持商业演示的简洁和高级感
- 可以把可信来源做成 hover 注释、轻标签、辅助说明、信息抽屉或页脚说明
- 如果某字段为空，不要编造新数字；可以展示为“待补充”“监测中”“将在中试阶段验证”

输出要求：
- 生成一套完整的桌面 Web Demo 高保真设计
- 请体现设计系统一致性，包括颜色、字体层级、卡片逻辑、图表语言、图标风格、按钮规范
- 请优先保证“讲故事能力、答辩说服力、商业价值感、系统完整度”
- 请不要把页面限制得过于模板化
- 请充分发挥你的设计审美和信息整合能力
- 请在真实数据基础上做高级、现代、具有产业科技感的表达

你接下来会收到 JSON 数据，请严格基于该 JSON 完成设计。
